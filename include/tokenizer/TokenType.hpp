#ifndef TOKEN_TYPE_HPP
#define TOKEN_TYPE_HPP

enum TokenType
{
  // SYMBOLS
  LEFT_BRACE,
  RIGHT_BRACE,
  SEMICOLON,

  // KEYWORDS
  SERVER,
  LISTEN,
  ROOT,
  INDEX,
  LOCATION,
  ERROR_PAGE,
  ALLOW_METHODS,
  AUTO_INDEX,
  CGI_PASS,
  CGI_EXTENSIONS,
  UPLOAD_DIR,
  CLIENT_MAX_BODY_SIZE,

  // LITERALS
  IDENTIFIER,

  // SPECIAL
  END_OF_FILE,
  UNKNOWN
};

#endif