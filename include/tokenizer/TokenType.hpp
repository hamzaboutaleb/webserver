#ifndef TOKEN_TYPE_HPP
#define TOKEN_TYPE_HPP

enum TokenType
{
  // SYMBOLS
  LEFT_BRACE,
  RIGHT_BRACE,
  SEMICOLON,

  // KEYWORDS
  SERVER,
  LISTEN,
  SERVER_NAME,
  ROOT,
  INDEX,
  LOCATION,
  ERROR_PAGE,
  METHODS,
  AUTO_INDEX,
  CGI_EXTENSION,
  UPLOAD_STORE,
  CLIENT_MAX_BODY_SIZE,
  RETURN,

  // LITERALS
  IDENTIFIER,

  // SPECIAL
  END_OF_FILE,
  UNKNOWN
};

#endif